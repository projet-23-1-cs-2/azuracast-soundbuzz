apiVersion: v1
kind: Service
metadata:
    name: "kluster-a700"
    labels:
        app: "kluster-a700"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kluster-a700"